# npm run setup generates a private .env.local with a random secret.
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
DATABASE_PATH=./data/hockey-hub.sqlite

# Hosted deployments use PostgreSQL. Leave unset for local SQLite development.
DATABASE_URL=
